DATABASE_URL=
NEXT_PUBLIC_APP_URL=http://localhost:3000
LEGACY_API_BASE_URL=https://antarestar-dashboard.vercel.app
NEXT_PUBLIC_LEGACY_API_BASE_URL=

# Google Sheets API key — required to sync sheets with "Prevent download" restriction enabled.
# Get it: console.cloud.google.com → APIs → Google Sheets API → Credentials → Create API Key
# Restrict key to: Google Sheets API only (for security)
GOOGLE_SHEETS_API_KEY=

